{
  "name": "token-profiler",
  "version": "0.1.0",
  "description": "Context exposure profiler for agent prompts.",
  "type": "module",
  "bin": {
    "token-profiler": "./src/cli.js"
  },
  "scripts": {
    "test": "node --import tsx --test test/*.test.js",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "demo": "node src/cli.js demo",
    "summarize": "node src/cli.js summarize"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "tiktoken": "^1.0.22",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
