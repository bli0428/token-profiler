# Surfaces

The surfaces layer presents analyzer outputs as CLI output, HTTP responses, or
other exported views.

Use this layer when shaping analyzed data for a human or client-facing surface.

For boundary rules and allowed inputs/outputs, see [contract.md](contract.md).
